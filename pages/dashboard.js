import React from 'react';

const Dashboard = ({address}) => {
    return (
        <div className="">
            <h1>Dashboard</h1>
            <h1>{address}</h1>
        </div>
    );
};

export default Dashboard;
